import React from 'react';
import { createRoot } from 'react-dom/client';
import RenewalPipeline from './App';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RenewalPipeline />
  </React.StrictMode>
);
